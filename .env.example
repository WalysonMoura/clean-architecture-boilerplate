NODE_ENV=dev

# A fake database URL (typically a PostgreSQL URL)
DATABASE_URL=postgresql://docker:docker@localhost:5432/docker

# Fake JWT keys (these are just random strings for the purpose of example)
JWT_PRIVATE_KEY=fake-private-key-string
JWT_PUBLIC_KEY=fake-public-key-string

# Fake Cloudflare account ID (a random UUID format)
CLOUDFLARE_ACCOUNT_ID=123e4567-e89b-12d3-a456-426614174000

# Fake AWS bucket name
AWS_BUCKET_NAME=my-fake-bucket

# Fake AWS access keys (random alphanumeric strings)
AWS_ACCESS_KEY_ID=FAKEAWSACCESSKEYID12345
AWS_SECRET_ACCESS_KEY=FakeSecretAccessKey67890

# Optional Redis settings with defaults
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_DB=0

# Optional server port with default
PORT=3333
